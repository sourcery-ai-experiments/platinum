// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/platinum",
    output: 'export',
    env: {
        basepath: '/platinum',
    }
}

module.exports = nextConfig;
